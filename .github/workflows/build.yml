name: Build PS4 Plugin

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Clang & Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld llvm

      - name: Compile Plugin
        run: |
          clang -target x86_64-pc-freebsd9-elf -O2 -fPIC -shared \
            src/font.c \
            src/portal_protocol.c \
            src/ui_overlay.c \
            src/main.c \
            -o ps4_figure_emu.prx

      - name: Upload PRX Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ps4_figure_emu
          path: ps4_figure_emu.prx
